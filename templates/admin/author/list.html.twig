{% extends 'base.html.twig' %}
{% block title %}Liste des autheurs{% endblock %}

{% block body %}
  

    <h1> Liste des authrurs: </h1>

     {% for author in authors %}
        <h3> {{ author.name }} </h3>
    
        {# <a href="{{ path('app_author_update', {id: author.id})}}">modifier</a> #}
     {% endfor %}
{% endblock %}